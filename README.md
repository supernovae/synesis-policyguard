# Synesis PolicyGuard

This project was generated with **Synesis** using **Claude Code** connected to **Synesis Corder API**, with the `POLICYGUARD_PLAN.md` as the planner and resulting code was generated into `/policyguard` for review.

## Generation Context

The original prompt that generated the Claude Code plan (`POLICYGUARD_PLAN.md`) and resulting code can be found in [PROMPT.md](PROMPT.md).